# Run Log — Bloque 2 (Conversión + Fix Google Tag)

- **Fecha:** 2026-05-27
- **Repo:** bosco-blanco/ecb.com
- **Rama:** claude/web-analysis-improvement-plan-cYu8I
- **Store:** eb9117-4.myshopify.com
- **Modo:** Autopiloto total
- **Live theme:** ecb-mejoras-auditoria (ID 198027215185)

---

## Estado inicial
- Admin API token caducado (expiraba 2026-05-26) → **refrescado OK** vía client_credentials, guardado en `~/.auto-memory/secrets/shopify-admin-api.env`.
- Theme Access token disponible (`SHOPIFY_CLI_THEME_TOKEN`).
- Snippets existentes relevantes: `wine-product-info.liquid`, `visual-polish.liquid`, `mobile-ux-enhancements.liquid`, `trust-badges.liquid`.
- **NO existen:** `sticky-atc.liquid`, `age-gate.liquid`, `trust-bar.liquid`, `related-products.liquid`.

---

## BLOQUE A — FIX GOOGLE TAG

### Diagnóstico
Grep en `layout/theme.liquid` + repo-wide:
- `G-EEFRE4XFMF` → **NO aparece** en ningún `.liquid` del tema.
- Lo único hardcodeado es el contenedor **GTM-NPCDS6X** (líneas 4-10 de `theme.liquid`), que es el contenedor **zombie** que el prompt prohíbe tocar. Está colocado al principio del `<head>`, ANTES de `<meta charset="utf-8">`.

Verificación en producción (`curl https://www.encopadebalon.com/`):
- `G-EEFRE4XFMF` aparece **8 veces**, todas dentro de un JSON de configuración del **Web Pixel Manager nativo de Shopify** (`wpm`), bajo `"google_tag_ids":["G-EEFRE4XFMF","AW-860467003","GT-NNVDP65S"...]` con `gtag_events` (begin_checkout, view_item, purchase, page_view, add_payment_info...).
- **NO existe** ningún `<script src="...googletagmanager.com/gtag/js?id=G-EEFRE4XFMF">` hardcodeado en el head.

### Conclusión → CAMINO (b)
El tag GA4 `G-EEFRE4XFMF` lo inyecta la **integración nativa Shopify → Google & YouTube** a través del sistema de Customer Events / Web Pixel Manager (sandbox asíncrono), **no** como snippet en `<head>`.

**Matiz importante:** la premisa del prompt ("gtag.js cargándose en posición demasiado baja del head") no aplica tal cual: GA4 no se carga vía snippet de head sino vía Web Pixel Manager de Shopify (cargado de forma asíncrona y sandboxeada por diseño). No es reposicionable editando `theme.liquid`.

### Acción tomada
**NINGUNA edición de código.** Fuera de scope de Claude Code (decisión humana).

### Instrucción para Bosco
El snippet de GA4 (G-EEFRE4XFMF) lo gestiona la integración nativa Shopify→Google (Web Pixel Manager). No se reposiciona editando el tema. Opciones si se quiere control de posición/carga:
1. **(Recomendado) Dejarlo como está.** El Web Pixel Manager de Shopify ya carga GA4 de forma asíncrona y resiliente; reposicionarlo manualmente puede duplicar eventos y romper el consent mode nativo. La "cobertura 77,5%" probablemente se deba a consent mode / adblockers, no a la posición del tag.
2. Si aún así se quiere snippet manual: instalar gtag.js a mano en `theme.liquid` (primera línea tras `<meta charset>`) **y** desactivar la inyección automática en Admin → Apps → Google & YouTube → Settings (si no, se duplican eventos). **Decisión humana.**

**Sobre el GTM-NPCDS6X zombie:** sigue hardcodeado al principio del head de `theme.liquid` (líneas 4-10). El prompt indica NO tocarlo, así que se deja. (Nota humana: si de verdad es zombie y no se usa, conviene eliminarlo en el futuro porque carga gtm.js síncrono al inicio del head y penaliza CWV/INP — pero eso es decisión humana, fuera de scope.)

**Estado Bloque A: COMPLETADO (camino b, sin cambios de código).**

---

## BLOQUE B — STICKY ADD TO CART (mobile)

### Diagnóstico
- `snippets/sticky-atc.liquid` **NO existía** (ni sticky-add-to-cart/mobile-atc).
- `snippets/mobile-ux-enhancements.liquid` tiene una "sticky bottom nav" pero es **navegación del sitio**, no ATC de producto.
- Botón ATC principal en `snippets/buy-buttons.liquid`: `id="ProductSubmitButton-{{ section_id }}"` dentro de `<product-form>`.

### Acción → CREADO desde cero
`snippets/sticky-atc.liquid`:
- `position:fixed; bottom:0; width:100%; padding:10-12px`.
- Visible SOLO < 990px (media query).
- Visible SOLO cuando el ATC principal sale de viewport → **IntersectionObserver** sobre `ProductSubmitButton-{id}`.
- Muestra imagen 48px + título + precio + botón "Añadir al carrito".
- El botón sticky hace `mainBtn.click()` → reutiliza exactamente el flujo AJAX add-to-cart existente (product-form.js). Sin lógica de carrito duplicada.
- `z-index:1001` (sobre la bottom-nav `ecdb-mobile-nav` z-999 y widgets de chat).
- **Anti-solape:** mide por JS la altura de `.ecdb-mobile-nav` (visible en móvil ≤749px) y desplaza el sticky con `bottom = navHeight` para no taparla.
- `MutationObserver` sincroniza precio y estado `disabled` al cambiar variante.
- Renderizado en `sections/main-product.liquid` (bloque buy_buttons): `{% render 'sticky-atc', product: product, section_id: section.id %}`.

## BLOQUE C — SCHEMA PRODUCT ENRIQUECIDO

### Diagnóstico
- Fuente activa de JSON-LD Product: `sections/main-product.liquid` (línea ~1190).
- `snippets/booster-seo.liquid` también tiene un Product JSON-LD con aggregateRating **pero NO se renderiza en ningún sitio** (no hay `render`), así que no es la fuente activa → no se toca (evitar schema duplicado).
- El JSON-LD activo ya tenía: name, url, image, description, sku, brand, offers[] por variante (sku, gtin, availability, price, priceCurrency, url).

### Acción → ENRIQUECIDO
Añadido al JSON-LD activo:
- `"mpn"` = sku (junto al sku existente).
- En cada offer: `"priceValidUntil"` = `{año+1}-12-31`, `"seller"` = Organization "En Copa de Balón".
- `"aggregateRating"` **condicional**: solo si `reviews.rating_count > 0`. Usa `reviews.rating.value.rating` (ratingValue), `scale_max` (bestRating), `rating_count` (reviewCount).
  - Decisión: se usan los metafields `reviews.*` (namespace que **Judge.me** puebla y que el tema ya usa en todo el código), en vez de parsear `judgeme.badge` — más fiable y consistente.
  - **Verificado:** hoy `reviews.rating_count` = null/0 en productos → aggregateRating NO se emite (Google rechaza rating con 0 reseñas). Se auto-activará cuando lleguen reseñas.

## BLOQUE D — CROSS-SELL "TAMBIÉN TE GUSTARÁ…"

### Diagnóstico
- No existía `related-products.liquid`.
- **Hallazgo clave (vía Admin API):** cada producto pertenece a colecciones que mapean 1:1 con sus metafields:
  - bodega → colección con título == `custom.bodega` (ej. "CVNE...", 9 prod)
  - D.O. → colección título == `custom.denominaci_n_de_origen` (ej. "Rioja", 309 prod)
  - uva → colección título == `custom.tipo_de_uva` (ej. "Tempranillo", 457 prod)
- Esto permite matching limpio y **performante** (localizar colección por título en `product.collections`, máx ~20 iter) sin queries pesadas a 2.585 productos.

### Acción → CREADO
- `snippets/related-products.liquid` (orquestador) + `snippets/related-product-card.liquid` (card ligera propia).
- Lógica por fases con cap=6, dedup por ids y `limit:` por colección:
  1. misma bodega → 2. misma D.O. → 3. misma uva → 4. precio ±25% (colección amplia "vinos"/"todos-los-productos").
- Card: imagen + título (2 líneas) + precio + **badge** (D.O. o tipo de vino).
- Grid responsive: **2 col móvil / 4 col desktop** (≥990px).
- Título: "Si te gusta este vino, también te gustará...".
- Renderizado en `sections/main-product.liquid` debajo de las columnas del producto (full-width, tras la descripción), antes del desctab.
- Solo se pinta la sección si hay ≥1 relacionado (capture + guard).

## BLOQUE E — TRUST MICROCOPY JUNTO A ADD TO CART

### Acción → AÑADIDO
- En `sections/main-product.liquid`, bloque buy_buttons, **justo debajo** del render de `buy-buttons` (botón ATC): div `.trust-microcopy` con los 4 signals (📦 embalaje, ❄️ cadena de frío, 🚚 envío 24-48h, ↩️ devolución gratis).
- Responsive vía clase + media query: **2 col móvil / 4 col desktop** (el inline original no soporta media queries).
- Nota: el snippet `trust-badges` (más abajo) ya cubre seguridad/SSL/experiencia con cierto solape en embalaje/cadena de frío. El microcopy nuevo prioriza posición de conversión (pegado al botón) + logística/devolución. (Recomendación humana: revisar a futuro si conviene recortar el solape de trust-badges.)

## BLOQUE F — JUDGE.ME REVIEWS (preparación)

### Diagnóstico
- Infraestructura de reseñas YA presente en `sections/main-product.liquid`:
  - Bloque `rating` (resumen de estrellas, condicional a `reviews.rating.value`).
  - `#shopify-product-reviews` (widget completo) en la zona desctab (`enable_desctab`).
  - Metafields `reviews.rating` / `reviews.rating_count` que Judge.me sincroniza.

### Decisión (conservadora, autopiloto)
- Reubicar el widget completo de reseñas a justo debajo del trust microcopy es **invasivo** (relocaliza el tab del fondo a la columna de info) y, con **0 reseñas hoy**, arriesga duplicar la auto-inyección de Judge.me sin beneficio visible. → **No se reubica el widget completo.**
- La parte accionable y de bajo riesgo ya queda cubierta: el **aggregateRating del Bloque C se auto-activará** cuando lleguen reseñas (~4-6 semanas), sin más cambios de código.
- **Instrucción para Bosco:** cuando Marketing lance la reactivación con cupón y empiecen a entrar reseñas, decidir manualmente en el editor de tema el orden/posición del bloque `rating` y del widget Judge.me (es configuración por bloques del theme editor, no código de sección). Decisión humana.

---

## Validación

- `shopify theme check` (vía `npx @shopify/cli@latest`) → corrió completo (heap 6 GB). **0 offenses** en `sticky-atc.liquid`, `related-products.liquid`, `related-product-card.liquid`, `sections/main-product.liquid`.
- Balance de tags Liquid (verificación local) OK en los 4 ficheros.

## Push al tema (live, ID 198027215185 `ecb-mejoras-auditoria`)

- El token Theme Access (`atkn_…`) devolvió **401** al usarlo desde Shopify CLI 4.0.
- Plan B (funcionó): subida vía **Admin REST Asset API** (PUT `themes/{id}/assets.json`) con el Admin token (shpat_) — equivale a `--only --nodelete` (PUT solo crea/actualiza los assets indicados, no borra otros).
- Assets subidos (200 OK):
  - `sections/main-product.liquid` (97 624 bytes)
  - `snippets/sticky-atc.liquid` (6 303 bytes)
  - `snippets/related-products.liquid` (5 544 → 6 123 bytes tras fix)
  - `snippets/related-product-card.liquid` (1 332 bytes)

## Bug encontrado y corregido durante validación

- **Bloque D no renderizaba inicialmente** (0 candidatos). Causa raíz: precedencia ambigua de filtro Liquid en `if ct == bodega | downcase` (combinada con `and`).
- Fix: precomputar `bodega`, `do_val`, `uva` ya en minúsculas, y cambiar el guard del wrapper a `if related_grid contains 'related-card'` (no depende del scope de `assign` dentro de `capture`).
- Verificado tras fix: 6 cards (Cune Imperial Reserva 2020, Cune Reserva 2019, Magnum Cune Imperial Reserva 2020, Magnum Cune Reserva 2019, Viña Ardanza Reserva 2019, Luis Cañas Crianza 2022) — phase 1 (CVNE bodega) + phase 2 (Rioja D.O.).

## Hallazgo importante (humano, decisión fuera de scope)

En `/products/cune-crianza-2021` hay **dos** JSON-LD `@type=Product` en producción:
1. Inyectado por la **app Booster SEO** (no es el snippet `booster-seo.liquid` del tema — ese no se renderiza por código). Incluye `weight`, `itemCondition`, `shippingDetails`, seller con descripción/logo, `priceValidUntil` a 90 días.
2. El del tema (`sections/main-product.liquid`) enriquecido en este bloque: `mpn`, `priceValidUntil` (año+1, 31-dic), `seller` Organization, `aggregateRating` condicional.

Dos schemas Product en la misma página es subóptimo (Google deduplicará pero puede haber inconsistencias). **Decisión humana:** elegir UNA fuente — o desactivar el schema de Booster SEO en la app, o quitar el JSON-LD del tema. Recomendación: dejar el del tema (controlado por nosotros, con `aggregateRating` que activará Judge.me) y desactivar el de Booster.

## Verificación post-push en `/products/cune-crianza-2021`

| Bloque | Marcador | Cuenta live |
|---|---|---|
| B sticky | `ecb-sticky-atc` | 11 (CSS+HTML) |
| C mpn | `"mpn"` | 2 (1 del tema + 1 Booster) |
| C priceValidUntil | `priceValidUntil` | 2 |
| C seller | `"seller"` | 2 |
| C aggregateRating | esperado 0 (0 reseñas hoy) | 0 ✅ |
| D sección | `related-products__title` | 1 |
| D cards | titles renderizados | 6 |
| D headline | "también te gustará" | 1 |
| E microcopy | container `trust-microcopy` | 1 + 4 items ✅ |

---

## REPORTE FINAL

### 1. Tabla resumen

| Bloque | Estado | Cambios | Commit | Notas |
|---|---|---|---|---|
| A — Fix Google Tag | ✅ COMPLETADO (camino b) | Sin código | — | G-EEFRE4XFMF lo inyecta Web Pixel Manager nativo, no es snippet de head reposicionable. GTM-NPCDS6X zombie se deja por instrucción. |
| B — Sticky ATC | ✅ DESPLEGADO | `snippets/sticky-atc.liquid` (nuevo) + render en main-product | 6d5f371 + 3d3c7bb | <990px, IntersectionObserver, anti-solape con mobile-nav, reusa flujo AJAX existente. |
| C — Schema Product | ✅ DESPLEGADO | `sections/main-product.liquid` (JSON-LD) | 3d3c7bb | +mpn, +priceValidUntil, +seller, +aggregateRating condicional (auto-activa con Judge.me). |
| D — Cross-sell | ✅ DESPLEGADO | `snippets/related-products.liquid` + `snippets/related-product-card.liquid` + render | e4f8b25 + c392bf8 | Matching por colección (bodega → D.O. → uva → precio ±25%). Verificado: 6 cards en cune-crianza-2021. |
| E — Trust microcopy | ✅ DESPLEGADO | `sections/main-product.liquid` (buy_buttons) | 3d3c7bb | 4 items, 2 col móvil / 4 col desktop. |
| F — Judge.me prep | ✅ DOCUMENTADO | Sin código | — | Widget+rating block ya existen; aggregateRating del Bloque C auto-activará. Reubicar widget completo = decisión humana cuando lleguen reseñas. |

### 2. Commits creados

Rama: `claude/web-analysis-improvement-plan-cYu8I` · Repo: `bosco-blanco/ecb.com`

| Hash | Mensaje | URL |
|---|---|---|
| 6d5f371 | feat(conv): bloque B — sticky add-to-cart en mobile | https://github.com/bosco-blanco/ecb.com/commit/6d5f371 |
| e4f8b25 | feat(conv): bloque D — cross-sell contextual relacionados | https://github.com/bosco-blanco/ecb.com/commit/e4f8b25 |
| 3d3c7bb | feat(conv): bloques C+E — JSON-LD Product enriquecido + trust microcopy | https://github.com/bosco-blanco/ecb.com/commit/3d3c7bb |
| 204b332 | docs(conv): bloque 2 — run-log (A diagnostico, B-F cambios) | https://github.com/bosco-blanco/ecb.com/commit/204b332 |
| c392bf8 | fix(conv): bloque D — corrige matching cross-sell (precedencia de filtro Liquid) | https://github.com/bosco-blanco/ecb.com/commit/c392bf8 |

### 3. MISSING-CREDS

No procede. El token Theme Access (atkn_) devolvió 401 desde Shopify CLI, pero el Admin token funcionó para todas las operaciones de push (assets + lectura de metafields). Si más adelante se quiere usar Shopify CLI nativo, conviene regenerar el Theme Access token en Admin → Apps → Theme Access.

### 4. Métricas a vigilar (7-14 días)

| Métrica | Hoy | Objetivo | Cómo |
|---|---|---|---|
| Add-to-cart rate | 2,19% | 3,5% | GA4 events `add_to_cart` / `view_item` en página producto. Impacto principal esperado: sticky ATC (B) + microcopy (E). |
| Conversión global | 0,78% | 1,2% | GA4 `purchase` / sesiones. Impacto combinado de todos los bloques. |
| INP móvil | 226 ms | <200 ms | CrUX / PageSpeed Insights. El gtag NO se reposicionó (causa real: WPM nativo + scripts pesados de apps); ver "Hallazgos humanos". |
| Cobertura GA4 | 77,5% | 90%+ | GA4 / consent mode. Probablemente NO mejorará con esta tanda — depende de consent banner y adblockers (ver hallazgo). |
| Rich snippets Product | — | aparece en SERP | search.google.com/test/rich-results sobre URLs producto. **Importante:** decidir antes si se queda el JSON-LD del tema o el de Booster SEO (ver "Hallazgo importante"). |
| Reseñas Judge.me | 0 | ~30 en 4-6 semanas | Una vez >0, aggregateRating auto-aparecerá en JSON-LD → rich snippets con estrellas. |

### 5. Descubrimientos / decisiones humanas

1. **Bloque A — Premisa parcialmente errónea.** El gtag.js NO está hardcodeado en theme.liquid: lo inyecta el Web Pixel Manager nativo de Shopify (canal Google & YouTube). No es reposicionable editando el tema. La "cobertura 77,5%" se debe casi seguro a consent mode / adblockers, NO a posición del tag.
2. **GTM-NPCDS6X zombie** sigue hardcodeado al inicio del head de `theme.liquid` (líneas 4-10), carga `gtm.js` síncrono. Si está confirmado como zombie, conviene eliminarlo en el futuro para mejorar CWV/INP. Decisión humana (el prompt prohibió tocarlo).
3. **Dos JSON-LD Product** en la misma página: el del tema (enriquecido por este bloque) y el inyectado por la app Booster SEO. Recomiendo desactivar el de Booster SEO y mantener solo el del tema, que controlas y que activa aggregateRating con Judge.me.
4. **Solape de trust signals.** El nuevo `trust-microcopy` (justo debajo del ATC) y el snippet existente `trust-badges` (más abajo) comparten parcialmente mensajes de embalaje y cadena de frío. Considera trimar `trust-badges` para que cubra solo lo que `trust-microcopy` no cubre (pago seguro, SSL, sumilleres expertos, +30 años).
5. **Judge.me / Bloque F.** Cuando lleguen reseñas, decide manualmente en el editor de tema el orden de los bloques `rating` y `description` y la posición del widget Judge.me. El aggregateRating en JSON-LD ya está listo para auto-activarse.

